排版整齐 -  控件高度一致, 左侧每行的输入框对齐

本周等按钮, 分页跳转, 共几页

提现订单 - 新增操作记录表(人工提现,审核等地方插入记录, orderId, adminId, operationType, note, addTime; 非人工提现显示为系统; 充值同理, 但目前不变, 流程未通); 控件对齐, 日期控件设置默认值

玩家订单查询:
    控件对齐, 日期控件用原来的, 设置默认值
    //所属代理 - (新需求这边是渠道包 - 发哥ios那种, 目前不做) (这个代理渠道包和张总之前提过的users和recharge_log, smc_agent_log的关系);



电玩城和平安注释

直接点击左侧, 参数怎么传递的

用户管理按照参考的后台, 包括查询条件; 分页加跳转; 用户权限设置改为未开放并只读; 用户游戏信息数据; 用户活动信息数据, 行为是登录游戏和输xx金豆;

用户管理 -> 卡死; 封用户失败, 并且状态变为被封禁; 封用户之后踢下线; 投注记录这种大数据,limit order by效率低下, 加默认时间

----------

jquery, ajax, vue, 设计模式, mysql